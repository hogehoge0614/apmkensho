---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-api
  namespace: demo-newrelic
  labels:
    app: payment-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-api
  template:
    metadata:
      labels:
        app: payment-api
        environment: demo-newrelic
      annotations:
        instrumentation.newrelic.com/inject-python: "newrelic"
    spec:
      serviceAccountName: newrelic-app-sa
      nodeSelector:
        node-type: ec2
      containers:
        - name: payment-api
          image: ${ECR_REGISTRY}/obs-poc/payment-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: SERVICE_NAME
              value: "payment-api"
            - name: ENVIRONMENT
              value: "demo-newrelic"
            - name: EXTERNAL_URL
              value: "http://external-api-simulator:8000"
            - name: NEW_RELIC_APP_NAME
              value: "payment-api"
            - name: NEW_RELIC_LABELS
              value: "environment:demo-newrelic;service:payment-api"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: payment-api
  namespace: demo-newrelic
spec:
  type: ClusterIP
  selector:
    app: payment-api
  ports:
    - port: 8000
      targetPort: 8000
      name: http
