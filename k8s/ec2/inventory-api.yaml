---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-api
  namespace: demo-ec2
  labels:
    app: inventory-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-api
  template:
    metadata:
      labels:
        app: inventory-api
        environment: demo-ec2
      annotations:
        instrumentation.opentelemetry.io/inject-python: "true"
    spec:
      serviceAccountName: app-signals-sa
      nodeSelector:
        node-type: ec2
      containers:
        - name: inventory-api
          image: ${ECR_REGISTRY}/obs-poc/inventory-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: SERVICE_NAME
              value: "inventory-api"
            - name: ENVIRONMENT
              value: "demo-ec2"
            - name: EXTERNAL_URL
              value: "http://external-api-simulator:8000"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://cloudwatch-agent.amazon-cloudwatch:4317"
            - name: OTEL_SERVICE_NAME
              value: "inventory-api"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "deployment.environment=demo-ec2,k8s.namespace.name=demo-ec2"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  namespace: demo-ec2
spec:
  type: ClusterIP
  selector:
    app: inventory-api
  ports:
    - port: 8000
      targetPort: 8000
      name: http
