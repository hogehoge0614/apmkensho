---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-api
  namespace: eks-ec2-appsignals
  labels:
    app: alert-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alert-api
  template:
    metadata:
      labels:
        app: alert-api
        environment: eks-ec2-appsignals
      annotations:
        instrumentation.opentelemetry.io/inject-python: "true"
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
    spec:
      serviceAccountName: app-signals-sa
      nodeSelector:
        node-type: ec2
      containers:
        - name: alert-api
          image: ${ECR_REGISTRY}/obs-poc/alert-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: SERVICE_NAME
              value: "alert-api"
            - name: ENVIRONMENT
              value: "eks-ec2-appsignals"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://cloudwatch-agent.amazon-cloudwatch:4316"
            - name: OTEL_LOGS_EXPORTER
              value: "none"
            - name: OTEL_SERVICE_NAME
              value: "alert-api"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "deployment.environment=eks-ec2-appsignals,k8s.namespace.name=eks-ec2-appsignals"
            - name: STATSD_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: STATSD_PORT
              value: "8125"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: alert-api
  namespace: eks-ec2-appsignals
  labels:
    app: alert-api
spec:
  type: ClusterIP
  selector:
    app: alert-api
  ports:
    - port: 8000
      targetPort: 8000
      name: http
