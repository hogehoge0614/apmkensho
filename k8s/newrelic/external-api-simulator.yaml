---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-api-simulator
  namespace: demo-newrelic
  labels:
    app: external-api-simulator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: external-api-simulator
  template:
    metadata:
      labels:
        app: external-api-simulator
        environment: demo-newrelic
      annotations:
        instrumentation.newrelic.com/inject-python: "newrelic"
    spec:
      serviceAccountName: newrelic-app-sa
      nodeSelector:
        node-type: ec2
      containers:
        - name: external-api-simulator
          image: ${ECR_REGISTRY}/obs-poc/external-api-simulator:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: SERVICE_NAME
              value: "external-api-simulator"
            - name: ENVIRONMENT
              value: "demo-newrelic"
            - name: NEW_RELIC_APP_NAME
              value: "external-api-simulator"
            - name: NEW_RELIC_LABELS
              value: "environment:demo-newrelic;service:external-api-simulator"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: external-api-simulator
  namespace: demo-newrelic
spec:
  type: ClusterIP
  selector:
    app: external-api-simulator
  ports:
    - port: 8000
      targetPort: 8000
      name: http
